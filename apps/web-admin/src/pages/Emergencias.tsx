import { PageHeader, EmptyState } from '../components/ui';

export default function Emergencias() {
  return (
    <div>
      <PageHeader title="Monitor de Emergencias en Vivo" subtitle="Panel central de control y despacho para triajes de emergencias en tiempo real." />
      <EmptyState>🚨 Módulo en construcción — próxima entrega.</EmptyState>
    </div>
  );
}
