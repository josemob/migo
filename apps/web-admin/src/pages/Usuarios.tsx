import { PageHeader, EmptyState } from '../components/ui';

export default function Usuarios() {
  return (
    <div>
      <PageHeader title="Usuarios & Mascotas (B2C)" subtitle="Monitoreo de dueños de mascotas registrados, historial de uso y suscripciones activas." />
      <EmptyState>👥 Módulo en construcción — próxima entrega.</EmptyState>
    </div>
  );
}
